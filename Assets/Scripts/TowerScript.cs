using UnityEngine;

/// <summary>
/// Legacy tower script. Superseded by TowerFactory + Turrent.
/// Kept to avoid compile errors on old scene objects.
/// </summary>
public class TowerScript : MonoBehaviour
{
    // No-op stub. TowerFactory handles all tower creation now.
}
